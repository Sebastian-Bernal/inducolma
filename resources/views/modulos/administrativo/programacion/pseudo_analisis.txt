// revisar existencias en la tabla items 
si existencias >= 0 
    entonces:
        return pregunta, si se usa;
    sino:
        //busca en tabla sobrantes
        sobrantes = Sobrantes::busqueda(largo, alto, ancho, tipo_madera);
        cantidad_items = 0;
        desperdicio = 0;
        busqueda = [];

        recorrer sobrantes

            cantidad_item = entero(ancho / ancho_item) * entero(alto/alto_item) + cantidad_item;
            desperdicio = ancho/ancho_item - entero(ancho/ancho_item) + alto/alto_item - entero(alto/alto_item) + desperdicio7
            busqueda [] = agregar cantidad_item, desperdicio
        termina el ciclo 

        return busqueda;


                
            

